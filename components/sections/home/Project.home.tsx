import SectionLayout from "@/components/layouts/SectionLayout";

const ProjectSection = () => {
  return (
    <section id="projects">
      <SectionLayout label="Things I've built" heading="Projects" />
    </section>
  );
};

export default ProjectSection;
